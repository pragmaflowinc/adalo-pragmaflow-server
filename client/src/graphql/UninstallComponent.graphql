mutation UninstallComponent($sessionToken: String!, $componentId: String!, $organizationId: String!) {
  uninstallComponent(sessionToken: $sessionToken, componentId: $componentId, organizationId: $organizationId)
}